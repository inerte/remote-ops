{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sigil:compile": "node scripts/sigil-compile.mjs",
    "sigil:test": "cd .. && sigil test",
    "dev": "npm run sigil:compile && vite",
    "build": "npm run sigil:compile && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "pixi.js": "^8.18.1"
  }
}
